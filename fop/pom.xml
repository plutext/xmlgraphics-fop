<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.docx4j</groupId>
  <artifactId>docx4j-fo-renderer</artifactId>
  <name>docx4j FO renderer</name>
  <description>docx4j FO renderer, based on Apache FOP 2.11 (the all-in-one artifact: core, events and util). This is a modified distribution derived from Apache FOP 2.11. It is maintained by Plutext/docx4j and is not an Apache Software Foundation release. Apache FOP is a trademark of the Apache Software Foundation.</description>

  <parent>
    <groupId>org.docx4j</groupId>
    <artifactId>docx4j-fo-renderer-parent</artifactId>
    <version>2.11-docx4j.1-SNAPSHOT</version>
  </parent>

  <dependencies>
    <!-- fop deps -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>docx4j-fo-renderer-util</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>docx4j-fo-renderer-events</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>docx4j-fo-renderer-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- test deps -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifest>
              <addClasspath>true</addClasspath>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
              <mainClass>org.apache.fop.cli.Main</mainClass>
            </manifest>
            <manifestEntries>
              <Automatic-Module-Name>org.apache.xmlgraphics.fop</Automatic-Module-Name>
              <SpecificationTitle>XSL-FO - Extensible Stylesheet Language</SpecificationTitle>
              <SpecificationVersion>1.1</SpecificationVersion>
              <SpecificationVendor>World Wide Web Consortium</SpecificationVendor>
              <SpecificationURL>http://www.w3.org/TR/xsl</SpecificationURL>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <phase>install</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}</outputDirectory>
              <includeArtifactIds>docx4j-fo-renderer-core,docx4j-fo-renderer-util,docx4j-fo-renderer-events</includeArtifactIds>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
