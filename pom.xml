<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.docx4j</groupId>
  <artifactId>docx4j-fo-renderer-parent</artifactId>
  <version>${revision}</version>
  <name>docx4j FO renderer parent</name>
  <description>docx4j FO renderer, based on Apache FOP 2.11 (parent). This is a modified distribution derived from Apache FOP 2.11. It is maintained by Plutext/docx4j and is not an Apache Software Foundation release. Apache FOP is a trademark of the Apache Software Foundation.</description>
  <packaging>pom</packaging>
  <url>https://github.com/plutext/xmlgraphics-fop/tree/docx4j-2.11</url>

  <properties>
    <!-- The single place the version is written. Every module takes it through
         <parent><version>${revision}</version>, and none declares a version of its own, so a
         release changes one line. This is docx4j's arrangement; see docx4j-core/pom.xml.
         It REQUIRES flatten-maven-plugin below: Maven publishes the original pom, not the
         effective one, so without flattening consumers would receive a literal ${revision}
         they cannot resolve, and could not resolve the parent either to find out what it
         means. -->
    <revision>2.11-docx4j.1</revision>
    <ant.version>1.10.14</ant.version>
    <antrun.plugin.version>1.8</antrun.plugin.version>
    <batik.version>1.19</batik.version>
    <build.helper.plugin.version>1.9.1</build.helper.plugin.version>
    <checkstyle.plugin.version>2.14</checkstyle.plugin.version>
    <commons.io.version>2.17.0</commons.io.version>
    <commons.logging.version>1.3.0</commons.logging.version>
    <compiler.plugin.version>3.11.0</compiler.plugin.version>
    <exec.plugin.version>1.4.0</exec.plugin.version>
    <findbugs.plugin.version>4.8.4.0</findbugs.plugin.version>
    <jar.plugin.version>3.1.1</jar.plugin.version>
    <java.version>8</java.version>
    <junit.version>4.13.2</junit.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.info.reports.plugin.version>2.8</project.info.reports.plugin.version>
    <release.plugin.version>2.5.2</release.plugin.version>
    <surefire.plugin.version>2.18.1</surefire.plugin.version>
    <war.plugin.version>3.3.2</war.plugin.version>
    <xml.plugin.version>1.0.1</xml.plugin.version>
    <xmlgraphics.commons.version>2.11</xmlgraphics.commons.version>
    <xmlunit.version>1.2</xmlunit.version>
    <jdk.path>${env.JAVA_HOME}</jdk.path>
  </properties>

  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <organization>
    <name>Plutext Pty Ltd</name>
    <url>https://www.docx4java.org/</url>
  </organization>

  <!-- Maven Central requires at least one developer. Mirrors docx4j's own pom. -->
  <developers>
    <developer>
      <name>Jason Harrop</name>
      <email>jason@plutext.org</email>
      <organization>Plutext</organization>
      <organizationUrl>http://www.plutext.com</organizationUrl>
    </developer>
  </developers>

  <modules>
    <!-- docx4j-fo-renderer: only the modules docx4j consumes are built; fop-sandbox,
         fop-servlet and the transcoders stay in the tree, unmodified and unbuilt, so that
         merges from upstream apply cleanly. -->
    <module>fop</module>
    <module>fop-core</module>
    <module>fop-events</module>
    <module>fop-util</module>
  </modules>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <configuration>
            <tags>
              <tag>
                <name>event.severity</name>
              </tag>
            </tags>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-checkstyle-plugin</artifactId>
          <version>${checkstyle.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler.plugin.version}</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${jar.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-project-info-reports-plugin</artifactId>
          <version>${project.info.reports.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>com.github.spotbugs</groupId>
          <artifactId>spotbugs-maven-plugin</artifactId>
          <version>${findbugs.plugin.version}</version>
        </plugin>        
      </plugins>
    </pluginManagement>

    <plugins>
      <!-- Publishes a self-contained pom in place of the original: the parent element removed,
           inherited values inlined, and ${revision} resolved to a literal. Mandatory because of
           the revision property above. Bound to process-resources so it runs before packaging,
           and to clean so the generated file does not linger.

           flattenMode ossrh keeps the elements Central requires: name, description, url,
           licenses, developers, scm. The dependency mode is left at its default, unlike docx4j
           which needs 'all': this parent declares no dependencies and no dependencyManagement,
           and all 25 of fop-core's dependencies carry explicit versions, so nothing is
           inherited that flattening could drop. Do not copy 'all' here - it would hoist every
           transitive into a direct dependency and stop consumers pruning subtrees they
           exclude. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <version>1.8.0</version>
        <configuration>
          <flattenMode>ossrh</flattenMode>
          <!-- Also flatten the parent, which the plugin skips by default for pom packaging.
               Without this the parent publishes as the original pom, carrying a literal
               ${revision} nobody can resolve. Nothing consumes it, because the children come
               out self-contained, but a permanently broken artifact on Central is worth one
               line to avoid. docx4j leaves this off; see the note in releasing.md. -->
          <updatePomFile>true</updatePomFile>
        </configuration>
        <executions>
          <execution>
            <id>flatten</id>
            <phase>process-resources</phase>
            <goals>
              <goal>flatten</goal>
            </goals>
          </execution>
          <execution>
            <id>flatten.clean</id>
            <phase>clean</phase>
            <goals>
              <goal>clean</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Publishing to Maven Central. Deliberately the same mechanism docx4j uses, down to
           the plugin version and the `ossrh` server id in ~/.m2/settings.xml, so that a first
           release from this repository fails or succeeds the way docx4j's releases already do.
           docx4j's pom carries a TODO to migrate to the Central Publisher Portal plugin
           (https://central.sonatype.org/publish/publish-portal-maven/); that migration is one
           task across both repositories, not a divergence to start here. -->
      <plugin>
        <groupId>org.sonatype.plugins</groupId>
        <artifactId>nexus-staging-maven-plugin</artifactId>
        <version>1.7.0</version>
        <extensions>true</extensions>
        <configuration>
          <serverId>ossrh</serverId>
          <nexusUrl>https://ossrh-staging-api.central.sonatype.com</nexusUrl>
          <autoReleaseAfterClose>true</autoReleaseAfterClose>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>custom-javac</id>
      <activation>
        <property>
          <name>jdk.path</name>
        </property>
      </activation>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-compiler-plugin</artifactId>
              <version>${compiler.plugin.version}</version>
              <configuration>
                <source>${java.version}</source>
                <target>${java.version}</target>
                <executable>${jdk.path}/bin/javac</executable>
                <fork>true</fork>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
    <profile>
      <id>java-8-api</id>
      <activation>
        <jdk>[9,)</jdk>
      </activation>
      <properties>
        <maven.compiler.release>${java.version}</maven.compiler.release>
      </properties>
    </profile>

    <!-- Maven Central requires a sources jar, a javadoc jar and a detached PGP signature for
         every artifact. Plugin versions match docx4j's for the reason in the build section.
         Signing key: -Dgpg.keyname=<key>, or gpg's default key if unset. Passphrase:
         -Dgpg.passphrase=..., which needs the loopback pinentry argument below. -->
    <profile>
      <id>release</id>
      <properties>
        <!-- Signing binds to the verify phase, and fop-core binds spotbugs:check there too, so
             a release dry run would drag the whole static analysis along with it. CI already
             runs checkstyle and spotbugs on every push, and a release is built from a committed
             tree that passed them, so re-running here buys nothing and costs minutes. Set as a
             profile property rather than a command-line flag so it is not something to
             remember. Checkstyle needs no equivalent: this build never binds it to a phase, CI
             invokes it as an explicit goal. -->
        <spotbugs.skip>true</spotbugs.skip>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
            <version>3.0.1</version>
            <executions>
              <execution>
                <goals>
                  <goal>jar-no-fork</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
            <version>3.1.0</version>
            <configuration>
              <!-- FOP's javadoc does not pass doclint. The event.severity tag this build
                   declares comes from pluginManagement above and must not be overridden. -->
              <additionalJOption>-Xdoclint:none</additionalJOption>
              <maxmemory>512m</maxmemory>
              <source>${java.version}</source>
              <detectJavaApiLink>false</detectJavaApiLink>
            </configuration>
            <executions>
              <execution>
                <id>attach-javadocs</id>
                <goals>
                  <goal>jar</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>1.5</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <scm>
    <connection>scm:git:https://github.com/plutext/xmlgraphics-fop.git</connection>
    <developerConnection>scm:git:git@github.com:plutext/xmlgraphics-fop.git</developerConnection>
    <url>https://github.com/plutext/xmlgraphics-fop/tree/docx4j-2.11</url>
    <tag>docx4j-2.11</tag>
  </scm>

  <repositories>
    <repository>
      <id>central</id>
      <name>Central Repository</name>
      <url>https://repo.maven.apache.org/maven2</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>apache.snapshots.https</id>
      <url>https://repository.apache.org/content/repositories/snapshots</url>
      <releases>
        <enabled>false</enabled>
      </releases>
      <snapshots>
        <enabled>true</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>jboss.org</id>
      <url>https://repository.jboss.org/nexus/content/repositories/thirdparty-releases/</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
  </repositories>

  <distributionManagement>
    <site>
      <id>${project.artifactId}-site</id>
      <url>${project.baseUri}</url>
    </site>
  </distributionManagement>

</project>
